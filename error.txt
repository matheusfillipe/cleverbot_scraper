if __name__ == "__main__":
    print("Start the conversation, press Ctrl-c to stop \n")
    try:
        last_response = 'Hello, how are you, baby?'
        curr_session = True
        while True:
            last_response = cleverbot(last_response, session=('ken' if curr_session == True else 'kennina'))
            #print(f"Bot: {cleverbot(input('>> '), session='main')}")
            curr_session = ~curr_session
    except KeyboardInterrupt:
        print("Exiting.")

